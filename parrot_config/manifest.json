{
  "name": "parrot_config",
  "description": "This is an alternate way to define your parrot commands in a way that supports combos, throttling, debounce, switching out configs easily without needing to create new modes",
  "version": "0.3.0",
  "contributes": {
    "settings": ["user.parrot_config_combo_window"],
    "actions": [
      "user.parrot_config",
      "user.parrot_config_event_register",
      "user.parrot_config_event_unregister",
      "user.parrot_config_format_display",
      "user.parrot_config_noise"
    ]
  },
  "depends": {}
}
